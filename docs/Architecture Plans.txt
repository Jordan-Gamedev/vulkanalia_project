Forward+ Pipeline
______________________

┌─────────────┐
│ GPU Culling │ → Fill visbuffers for cameras, lights, planar reflections, and reflection probes using frustum culling aabb testing
└──────┬──────┘
       ▼
┌──────────────┐
│ Light Culling│ → Reduce active lights per object with cluster-based culling
└──────┬───────┘
       ▼
┌─────────────┐
│ Gather Pass │ → Gather info used for Reflections and Shadows
└──────┬──────┘
       │
       ├── Planar Reflections gather
       └── Shadow gather pass (for SSS depth prepass + volumetric shadows)
       ▼
┌──────────────┐
│ Depth Prepass│ → SSS on important objects only + occluder depths
└──────┬───────┘
       ▼
┌─────────────┐
│ Base Pass   │ → Forward rendering of cullable geometry
└──────┬──────┘
       ▼
┌─────────┐
│ VXGI    │ → Render Voxel Global Illumination and Irradiance
└──────┬──┘
       ▼       
┌──────────────────────┐
│ Screen Space Shadows │ → Gather → Depth Prepass (SSS) → Render
└──────┬───────────────┘
       ▼
┌─────────────┐
│ Shadow Maps │ → High-res for close-to-medium distance geometry
└──────┬──────┘
       ▼
┌───────────────┐
│ Shadow Volumes│ → Medium-to-far + large-volumed objects (PSSM/CS)
└──────┬────────┘
       ▼
┌─────────────┐
│ Resolve Pass│ → Combine shadow depth/stencil into render target
└──────┬──────┘
       ▼
┌───────────────────┐
│ Planar Reflections│ → Render planar reflections
└──────┬────────────┘
       ▼
┌──────────────────────┐
│ Realtime Reflection  │ → Update/Read reflection probe & blend into scene
│    Probe             │
└──────┬───────────────┘
       ▼
┌────────────────┐
│ Post Processing│ → Tone mapping, color grading, FXAA/SSAO
└────────────────┘
       │
       ├── Bloom, Lens Flares
       └── Tone Mapping and Color Grading

Notes:

Culling:

       Scene-wide instance data buffer should have LoD info (also contains distance culling) and aabb info for compute shader to calculate culling and lod picking
       An IndirectDrawData megabuffer and a PerInstanceData megabuffer will be shared by all visbuffers--both [max active visbuffer count] * max capacity
              Visbuffers hold offsets to the megabuffers which vk::cmd_draw_indexed_indirect will use
              For instance, 16 maximum visbuffers with instance buffers having a capacity of 262_144 instances (4mb) will make an allocated megabuffer of 64mib,
                     and 32kib for an indirect buffer with a 1024 draw call capacity

Depth prepass only used on important objects (for SSS) and likely occluders/large objects

Shadow scheme:

    Screen Space Shadows for important geometry
    High Resolution shadow maps for close-to-medium distance geometry
    Shadow volumes for medium-to-far geometry as well as large-volumed geometry

Texture-based AO for the smooth cel-shaded rendering

There will be one realtime reflection probe following the player for great close-medium distance reflections,
while planar reflections will handle all the flat surface reflections

VXGI scheme:

       Use Sparse Voxel Octree Global Illumination (SVOGI)

       Toroidal Addressing: Use modular arithmetic (wrap-around addressing) in your shader lookups for the voxel grid center.
       This allows the physical memory allocation of the 3D texture to stay fixed while logically shifting around the player
       
       Partial Re-voxelization: Only re-voxelize the newly exposed slices/columns of the grid that enter the player's vicinity as they move,
       leaving the overlapping interior intact.

       Sparse Structures: Store your voxel representation in a Sparse Voxel Octree (SVO) or pointer-based lists.
       Empty space consumes zero or negligible leaf allocations,
       dropping memory overhead from gigabytes down to manageable megabytes depending on scene complexity

       Use clipmap cascades for better detail close to the camera

       Use wider voxel cones for smoother, less blocky diffuse lighting